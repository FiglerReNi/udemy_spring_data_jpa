CREATE TABLE author_uuid_string (
                        id varchar(36) NOT NULL,
                        first_name varchar(255) DEFAULT NULL,
                        last_name varchar(255) DEFAULT NULL,
                        PRIMARY KEY (id)
)ENGINE = INNODB;
