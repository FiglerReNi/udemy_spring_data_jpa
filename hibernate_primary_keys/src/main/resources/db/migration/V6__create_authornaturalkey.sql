CREATE TABLE author_natural_key (
                                    first_name varchar(255) NOT NULL,
                                    last_name varchar(255) DEFAULT NULL,
                                    PRIMARY KEY (first_name)
)ENGINE = INNODB;
